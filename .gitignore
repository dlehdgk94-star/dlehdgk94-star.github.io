# ── 이미지 원본 백업 (185MB JPG/PNG — WebP만 커밋) ──
images/_original_backup/

# ── 환경변수 / 시크릿 (절대 공개 저장소에 올리면 안 됨) ──
.env
.env.*
*.env
.env.local
.env.production
.env.staging

# ── OS 시스템 파일 ──
.DS_Store
Thumbs.db
desktop.ini

# ── Node.js ──
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── 로그 ──
*.log

# ── Supabase 임시 파일 ──
supabase/.temp/
